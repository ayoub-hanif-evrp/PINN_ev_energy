extends: base.yaml

experiment:
  profile: paper
  seed: 0
  seeds: [0, 1, 2]
  cross_traj_seeds: [0]
  methods:
    - constant
    - physics
    - elasticnet
    - weak_mlp
    - mlp_state
    - pinn_no_dynamics
    - pinn_fulltrip
    - pinn

training:
  max_epochs: 1200
  early_stopping_patience: 100
  lambda_physics_grid: [0.1]

scarcity:
  n_train_trips: [3, 5, 8, 12, 16]
  n_subset_repeats: 3
  seeds: [0]
